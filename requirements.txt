# Everything the verification suite needs. Any reasonably recent version works;
# the lower bounds below are the oldest releases the code has been run against.
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
openpyxl>=3.1
rdkit>=2023.3.1
pytest>=7.0

# Optional, only for the in-domain positive control
# (python -m npadmet.predict_wang). It pulls in a deep-learning stack, so it is
# not installed by default and the corresponding test skips without it.
# admet-ai>=1.3

# Reading the optional Kim et al. (2026) supporting table (section 3.7)
python-docx>=1.0

# The reference model used by the scaffold-split and transfer experiments
scikit-learn>=1.3
