# Generated by run_all.py
output/

# Inputs (see data/README.md). Only the README and the publication-status
# annotation, which ships with the code rather than with the Zenodo record, are tracked.
data/*
!data/README.md
!data/benchmark_publication_status.csv

# Python
__pycache__/
.pytest_cache/
.venv/
*.egg-info/

# The suite installs with loose version bounds; a lock file pinned to one machine
# would contradict that.
uv.lock
